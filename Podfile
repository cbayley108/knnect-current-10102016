# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'knnect' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for knnect
  pod 'Firebase'
  pod 'FirebaseAuth'
  pod 'Firebase/Database'
  pod 'Firebase/Core'
  pod 'JSQMessagesViewController'
  pod 'TagListView'
  pod 'FirebaseStorage'

  target 'knnectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'knnectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
